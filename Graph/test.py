from os.path import abspath
import glob
from pathlib import *
file_name = input('Введите название файла: ')

file = abspath(file_name)

#paths = sorted(Path('.').glob('**/'+file_name))
x`

# n = file
#q = n.replace('\\', '/')

print(file)